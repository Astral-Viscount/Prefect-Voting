# Prefect-Voting

Please create an google oauth project in google developer console and then add your client id in a .env file in your system and also add your home account as test users and make sure to add localhost:5000 as your main url and localhost:5000/login as your redirect javascript url.

Then in your system run "pip install -r requirements.txt" to get all the dependencies for this project.